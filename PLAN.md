# PLAN: パスワード生成ツール

## 1. 概要

指定した文字数と文字種条件（英大文字・英小文字・数字・記号）に基づいてランダムなパスワードを生成する静的単一ページアプリを作る。文字数はスライダーと数値入力欄の両方で指定でき、生成結果はワンクリックでクリップボードにコピーできる。サーバコード・外部 API・ビルドツールは使わず、`public/index.html` 1 ファイル（CSS/JS インライン）で完結させ、Cloudflare Workers の静的配信でそのまま動作させる。

## 2. 受け入れ条件

- [ ] 文字数スライダー（range）と数値入力欄（number）が双方向に同期し、指定した長さ（8〜64、初期値 16）ちょうどのパスワードが生成される
- [ ] 英大文字・英小文字・数字・記号の各チェックボックスをオフにすると、生成結果にその文字種の文字が一切含まれない。オンにした文字種は（長さがオン個数以上のとき）少なくとも 1 文字含まれる
- [ ] すべてのチェックボックスをオフにした場合はパスワードを生成せず、エラーメッセージを表示する
- [ ] 「コピー」ボタンを押すと表示中のパスワード文字列がクリップボードに格納され、コピー完了のフィードバック（文言変化など）が表示される
- [ ] favicon がインライン data URI（`<link rel="icon" href="data:image/svg+xml,...">`）で含まれ、`</body>` 直前ではなくメインコンテナ内の通常フロー最下部に AGENTS.md 指定マークアップの `apps.jozo.beer` フッターがある
- [ ] `npm run verify` が通る

## 3. 実装方針

### 構成・技術

- `public/index.html` 単一ファイル。CSS は `<style>`、JS は `<script>` にインライン記述。外部リソース・フレームワークなし（Vanilla JS）
- 乱数は `crypto.getRandomValues()` を使用。`Math.random()` は使わない。剰余による偏り（modulo bias）を避けるため棄却サンプリングでインデックスを選ぶ
- クリップボードは `navigator.clipboard.writeText()`（配信は HTTPS のため secure context 前提）。失敗時は `try/catch` でエラーメッセージ表示にフォールバック

### レイアウト

- ダーク系のセンタリングカード UI。body は `display:flex; flex-direction:column; align-items:center`（footer を通常フロー最下部に置くため column 必須）
- カード内の構成（上から）:
  1. タイトル（h1）
  2. 生成結果表示欄（readonly input）＋「コピー」ボタン
  3. 文字数指定（range スライダー＋number 入力、現在値表示）
  4. 文字種チェックボックス 4 つ（英大文字・英小文字・数字・記号。初期値は全てオン）
  5. 「生成」ボタン
  6. エラー／コピー完了メッセージ領域
- favicon は鍵・南京錠モチーフの SVG を data URI でインライン化
- footer は AGENTS.md 指定のマークアップをカード外・センタリング済みメインフロー末尾に配置（リンク先 `https://apps.jozo.beer`・テキスト `apps.jozo.beer` は固定、色はテーマに合わせ調整可）

### 主要関数（JS）

- `randomIndex(max)`: `crypto.getRandomValues` ＋棄却サンプリングで `0..max-1` の一様乱数を返す
- `generatePassword(length, charsets)`: 選択された各文字種から 1 文字ずつ先取りして必須文字を確保し、残りを全選択文字種の結合プールから選び、最後に Fisher–Yates シャッフル（乱数は `randomIndex`）で並び替えて返す。文字種が 1 つも選択されていなければ `null`（呼び出し側でエラー表示）
- `copyToClipboard()`: 表示中パスワードを `navigator.clipboard.writeText` でコピーし、ボタン文言を一時的に「コピーしました」に変更
- イベント配線: スライダー⇔数値入力の `input` 同期、チェックボックス・長さ変更・「生成」ボタンで即時再生成、初期表示時にも 1 回生成

### 検証

- `npm run verify` の通過（favicon・hub リンク・プレースホルダ除去を機械検査）
- ブラウザで手動確認: 長さ境界値（8・64）、各文字種オフ時の除外、全オフ時のエラー、コピー動作
